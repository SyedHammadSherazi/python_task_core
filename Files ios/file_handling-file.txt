To avoid getting an error, you might want to check if the file exists before you try to delete it:To avoid getting an error, you might want to check if the file exists before you try to delete it:
Write to an Existing File
To write to an existing file, you must add a parameter to the open() function:
"a" - Append - will append to the end of the file
"w" - Write - will overwrite any existing content
